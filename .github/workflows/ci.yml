name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        run: python -m pip install -e ".[dev]"
      - name: Test
        run: python -m pytest -q
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: CLI via npm launcher
        run: node bin/baton.js --version

  converter:
    runs-on: ubuntu-latest
    steps:
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo install --git https://github.com/wiltshirek/txcript --rev f7b6a9777aac3c882059a0f5a61984a2b842ad9c txcript-cli

  # Single required check for branch protection (matrix jobs become test (3.11), …).
  CI:
    needs: [test, converter]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: All test jobs passed
        run: test "${{ needs.test.result }}" = "success" && test "${{ needs.converter.result }}" = "success"
